import { PAGE_NOT_FOUND_URL } from "../utils/constants";

const PageNotFound = () => {
    return (
        <div>
            <img src={PAGE_NOT_FOUND_URL} alt="pagenotfound_img"/>
        </div>
    );
}

export default PageNotFound;